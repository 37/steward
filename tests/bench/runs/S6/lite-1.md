# steward 1.3.0

- Expand the keep-pair dictionary from 50 to 300 entries.
- Update the Claude hook to accept namespaced commands and `@` or `$` command forms.
- Reorder the README to show rules and customization before examples.
- Add side-by-side `off` and `strict` rule tables to the README.
