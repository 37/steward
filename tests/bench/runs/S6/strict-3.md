# Steward 1.3.0

- Expanded the keep-pair dictionary from 50 to 300 pairs.
- Updated the Claude hook to accept namespaced commands and `@` or `$` command forms.
- Reorganized the README to show rules and customization before examples.
- Added side-by-side `off` and `strict` rule tables to the README.
